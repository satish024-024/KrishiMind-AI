# KrishiMind AI - Dependencies (Railway/Production)

# ── CRITICAL: CPU-only PyTorch (saves ~1.8GB vs CUDA version) ──
--extra-index-url https://download.pytorch.org/whl/cpu
torch --index-url https://download.pytorch.org/whl/cpu

# Flask API (Dashboard backend)
flask>=3.0.0
flask-cors>=4.0.0

# Google Gemini AI
google-genai>=1.0.0

# AI/ML Libraries (CPU only)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
numpy>=1.24.0

# Environment Management
python-dotenv>=1.0.0

# Utilities
requests>=2.31.0

# Production Server (For Render/Railway)
gunicorn>=20.1.0
